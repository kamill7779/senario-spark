# Contracts

Shared contracts live here so frontend, Go services, and Python AI services can evolve without hidden coupling.

- `openapi`: HTTP API specifications.
- `protobuf`: RPC or event schemas if introduced.
- `schemas`: JSON schemas for persisted and delivered data structures.

