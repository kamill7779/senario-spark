# Backend

Go backend services live here.

The exact service split is intentionally not fixed yet. Keep backend runtime services under `services/` once their boundaries are clear.

- `pkg`: shared Go packages for backend services only.
